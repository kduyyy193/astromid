# Required for Gemini API (Google AI Studio / Google Cloud).
GEMINI_API_KEY=""

# Optional: public app URL (callbacks, absolute links, etc.).
APP_URL=""

# Optional: production origin without trailing slash. Used for canonical, Open Graph, JSON-LD.
# Example: VITE_SITE_URL=https://astromind.example.com
VITE_SITE_URL=""
